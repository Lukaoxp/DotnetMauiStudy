<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiConcepts.Pages.Controls.EditControls"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="EditControls">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="50">
            <!--<Entry
                x:Name="Entry1"
                CharacterSpacing="5"
                ClearButtonVisibility="WhileEditing"
                FontSize="Medium"
                HeightRequest="50"
                IsPassword="False"
                IsReadOnly="False"
                Keyboard="Text"
                MaxLength="20"
                Placeholder="Entry view"
                ReturnType="Default" />

            <Editor
                x:Name="Editor1"
                AutoSize="TextChanges"
                CharacterSpacing="1"
                FontSize="Large"
                Placeholder="view editor"
                PlaceholderColor="Black" />

            <StackLayout Padding="40" Spacing="40">
                <Label FontSize="Large" Text="ActivityIndicator" />
                <ActivityIndicator IsRunning="True" Color="Orange" />
            </StackLayout>

            <VerticalStackLayout>
                <ProgressBar Progress="0.5" />
                <ProgressBar Progress="0.8" ProgressColor="Orange" />
            </VerticalStackLayout>

            <VerticalStackLayout>
                <Label FontSize="Medium" Text="Progresso do projeto" />
                <ProgressBar
                    x:Name="progressBar"
                    Progress="0.25"
                    ProgressColor="Red" />
                <Button
                    Clicked="Button_Clicked"
                    FontSize="Medium"
                    Text="Progredir progressbar" />
            </VerticalStackLayout>-->

            <VerticalStackLayout>
                <ActivityIndicator
                    x:Name="activityIndicator1"
                    HorizontalOptions="Center"
                    IsRunning="{Binding IsBusy}"
                    VerticalOptions="Center"
                    Color="Orange" />
            </VerticalStackLayout>
            <VerticalStackLayout
                Padding="20"
                Spacing="2"
                VerticalOptions="CenterAndExpand">


                <Label
                    FontAttributes="Bold"
                    FontSize="20"
                    Text="Nome do Usuário"
                    TextColor="#555" />
                <Entry
                    FontSize="20"
                    Keyboard="Text"
                    Placeholder="Informe o nome do usuário"
                    TextColor="#555" />
                <Label
                    FontAttributes="Bold"
                    FontSize="20"
                    Text="Senha"
                    TextColor="#555" />
                <Entry
                    FontSize="20"
                    IsPassword="True"
                    Keyboard="Numeric"
                    Placeholder="Informe a senha"
                    TextColor="#555" />
            </VerticalStackLayout>
            <Button
                x:Name="BtnLogin"
                Clicked="BtnLogin_Clicked"
                Text="Login" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>